import { test, expect, Locator } from '@playwright/test';
import { assert } from 'console';

test('test', async ({ page }) => {
  // todo need to call download_snapshot function and make sure it does the right thing
  // use mocked NB page so this works in CICD
});